---
import Card from '@/components/ui/data-display/Card/Card.astro';
import Avatar from '@/components/ui/data-display/Avatar/Avatar.astro';

const testimonials = [
  {
    name: 'Gabriele Buchstein',
    role: 'Leitende Physiotherapeutin',
    company: 'Sauerlandklinik Hachen',
    quote:
      'Die Patienten sind begeistert und sehr motiviert. Das Pixformance-System ermöglicht uns eine individuelle Betreuung auf höchstem Niveau.',
  },
  {
    name: 'Christoph Schneider',
    role: 'Therapieleitung',
    company: 'Niederrhein-Klinik',
    quote:
      'Auf der Suche nach einer Möglichkeit, unsere Patienten optimal zu betreuen, sind wir auf Pixformance gestoßen. Die Technologie ist beeindruckend.',
  },
  {
    name: 'Heike Schaible',
    role: 'Inhaberin',
    company: 'Physiotherapie Praxis',
    quote:
      'Ich will mit Pixformance einen zweiten Selbstzahlerbereich aufbauen. Die Investition hat sich bereits nach kurzer Zeit ausgezahlt.',
  },
];
---

<section class="py-16 md:py-24 bg-background-secondary">
  <div class="container">
    <h2 class="font-display text-4xl md:text-5xl font-bold text-center mb-12">
      Erfolgsgeschichten
    </h2>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {
        testimonials.map((t) => (
          <Card variant="elevated" padding="lg">
            <div class="flex flex-col gap-4">
              <div class="flex items-center gap-3">
                <Avatar
                  fallback={t.name
                    .split(' ')
                    .map((n) => n[0])
                    .join('')}
                />
                <div>
                  <p class="font-semibold text-sm">{t.name}</p>
                  <p class="text-xs text-foreground-muted">{t.role}</p>
                </div>
              </div>
              <p class="text-sm italic">"{t.quote}"</p>
            </div>
          </Card>
        ))
      }
    </div>
  </div>
</section>
